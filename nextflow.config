profiles {

    // --- 1) Local execution ---
    local {
        process.executor = 'local'
    }

    // --- 2) Docker execution ---
    docker {
        process.executor = 'local'
        docker.enabled = true
        process.container = 'ensemblorg/ensembl-vep:release_115.2'
    }
}


